@model IEnumerable<NBDProject.Models.BusinessRule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.projectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.designBid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.productionPlanTotalCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.designPercent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.projectName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.designBid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.productionPlanTotalCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.designPercent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
